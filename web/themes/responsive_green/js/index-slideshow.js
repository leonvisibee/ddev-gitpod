(function ($) {
	Drupal.behaviors.event_slick = {
		attach: function (context, settings) {
			$(window).load(function() {
				// alert("**");
				// $('#block-views-testsliceview-block .slick-slider').slick({
					// autoplay: true
				// });
			});
		}
	};
})(jQuery);
